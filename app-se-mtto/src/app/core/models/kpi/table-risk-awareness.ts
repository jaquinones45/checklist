export interface TableRiskAwareness {
  activity: string;
  advance: number;
  executedCutOffMonth: number;
  inExecuting: number;
  metrics: string;
  projecedCutOfftMonth: number;
  scheduled: any;
}
